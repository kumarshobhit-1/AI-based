{
  "name": "ai-disaster-early-warning-platform",
  "version": "1.0.0",
  "description": "AI-Based Disaster Early Warning Platform - Early alerts for floods, earthquakes, and extreme weather",
  "private": true,
  "scripts": {
    "start:backend": "cd server && node index.js",
    "start:frontend": "cd client && npm start",
    "start:ml": "cd ml && python app.py",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../ml && pip install -r requirements.txt",
    "seed": "cd server && node seed.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["disaster", "early-warning", "AI", "flood", "earthquake", "weather"],
  "author": "Disaster Warning Team",
  "license": "MIT"
}
